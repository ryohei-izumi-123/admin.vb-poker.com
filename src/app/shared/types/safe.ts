import {
  SafeHtml,
  SafeStyle,
  SafeScript,
  SafeUrl,
  SafeResourceUrl,
} from '@angular/platform-browser';
export type TSafe =
  | SafeHtml
  | SafeStyle
  | SafeScript
  | SafeUrl
  | SafeResourceUrl;
