/**
 *
 *
 * @export
 * @interface ICardLayoutOptions
 */
export interface ICardLayoutOptions {
  hasSignpost?: boolean;
  hasHeader?: boolean;
  hasTitle?: boolean;
  hasText?: boolean;
  hasFooter?: boolean;
  useGrid?: boolean;
}
