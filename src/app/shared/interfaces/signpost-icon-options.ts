import { IIconOptions } from '@shared/interfaces/icon-options';
import { TPosition } from '@shared/types';
export interface ISignpostIconOptions {
  clazz?: string;
  icon?: IIconOptions;
  position?: TPosition;
}
